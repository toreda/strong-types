import {AutoMap} from '../../src/auto/map';

interface SampleData {
	a: string;
}

describe('AutoMap', () => {
	let instance: AutoMap<SampleData>;

	beforeAll(() => {
		instance = new AutoMap<SampleData>();
	});

	describe('Constructor', () => {

	});

	describe('Implementation', () => {

	});
});
